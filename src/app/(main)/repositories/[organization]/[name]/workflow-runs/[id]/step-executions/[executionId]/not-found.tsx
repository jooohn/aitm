import NotFoundMessage from "@/app/components/NotFoundMessage";

export default function StepExecutionNotFound() {
  return (
    <NotFoundMessage>This step execution no longer exists.</NotFoundMessage>
  );
}
